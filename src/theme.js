// Tokens de tema — Adega Selecionada
// Paleta clara, uma única família tipográfica (sem serifa), pensada para leitura
// confortável em tela de celular: contraste alto entre texto e fundo, divisórias
// finas no lugar de cards encaixotados, cor de destaque usada com parcimônia.

export const C = {
  bg: "#F7F4ED",        // fundo da página, papel claro
  surface: "#FFFFFF",    // superfícies elevadas (painel do carrinho, inputs)
  ink: "#1E1B17",        // texto principal, quase preto grafite
  inkSoft: "#6B6357",    // texto secundário (origem, tipo, labels)
  muted: "#A79E8E",      // texto terciário (contadores, placeholders)
  line: "#E5DDCB",       // divisórias e bordas
  accent: "#6E2A2A",     // vinho bordô: preço, CTA principal
  accentSoft: "#F1E2DC", // fundo suave para destaque de preço/promoção
  gold: "#93732E",       // dourado fosco: selo de promoção/novidade
  danger: "#A23E3E",
  success: "#3E7A4E",
};

export const FONT = "'DM Sans', sans-serif";

// espaçamento em grade de 8px — use estes valores em vez de números soltos
export const SPACE = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32, xxl: 48, xxxl: 64 };
